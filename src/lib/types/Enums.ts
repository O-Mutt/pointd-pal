export enum EnabledSettings {
  ENABLED = 'Enabled',
  DISABLED = 'Disabled',
}

export enum PromptSettings {
  ALWAYS = 'Always',
  PROMPT = 'Prompt',
  NEVER = 'Never',
}

export enum ReadyState {
  DISCONNECTED = 0,
  CONNECTED = 1,
  CONNECTING = 2,
  DISCONNECTING = 3,
}
